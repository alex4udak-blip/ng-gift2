# 📊 Инструкция по настройке UTM меток для Nigerian MTN Landing Page

## 🎯 Общая информация

Этот лендинг автоматически перехватывает UTM параметры из URL и передает их в Keitaro трекер для корректного отслеживания конверсий и статистики.

## 📋 Поддерживаемые параметры

### Входящие параметры (из рекламной сети):

| Параметр | Описание | Пример |
|----------|----------|---------|
| `zoneid` | ID зоны в рекламной сети | `zoneid=12345` |
| `cost` | Стоимость клика | `cost=0.05` |
| `clickid` | Уникальный ID клика | `clickid=abc123xyz` |
| `bannerid` | ID баннера/креатива | `bannerid=456` |
| `campaignid` | ID кампании | `campaignid=789` |
| `keyword` | Ключевое слово (опционально) | `keyword=casino` |
| `sub_id_1` | Дополнительный sub ID | `sub_id_1=test` |

### Исходящие параметры (в Keitaro):

| Keitaro параметр | Источник | Описание |
|------------------|----------|----------|
| `source` | **zoneid** | ID зоны (маппится автоматически) |
| `cost` | cost | Стоимость клика |
| `currency` | hardcoded | **usd** (жестко прописан) |
| `external_id` | clickid | Уникальный ID клика |
| `creative_id` | bannerid | ID креатива |
| `ad_campaign_id` | campaignid | ID рекламной кампании |
| `keyword` | keyword | Ключевое слово |
| `sub_id_1` | sub_id_1 | Дополнительный sub ID |

## ⚠️ ВАЖНО: Маппинг zoneid → source

**Критически важно понимать:**
- Рекламная сеть передает параметр `zoneid`
- Keitaro ожидает параметр `source`
- Лендинг **автоматически** переименовывает `zoneid` в `source`
- Оригинальный параметр `zoneid` удаляется из финального URL

Это исправляет проблему, когда Keitaro получал домен ленда вместо реального значения зоны.

## 🔧 Настройка в PropellerAds

### Пример URL для PropellerAds:

```
https://www.dailyrewards.site/?clickid=${SUBID}&campaignid={campaign_id}&cost={cost}&zoneid={zone_id}&bannerid={banner_id}
```

### Расшифровка макросов PropellerAds:

- `${SUBID}` — уникальный ID клика PropellerAds
- `{campaign_id}` — ID кампании в PropellerAds
- `{cost}` — цена за клик
- `{zone_id}` — ID зоны размещения
- `{banner_id}` — ID баннера/креатива

**PropellerAds автоматически заменит эти макросы на реальные значения.**

## 🔧 Настройка в Keitaro

### В настройках потока в Keitaro:

1. **Поле "Сайт" (Site/Source):**
   ```
   source = {zoneid}
   ```
   ⚠️ Обратите внимание: в Keitaro используется `{zoneid}` как **макрос Keitaro**, но лендинг передает параметр `source`

2. **Остальные поля:**
   - External ID: `{external_id}`
   - Cost: `{cost}`
   - Currency: `{currency}` (всегда будет USD)
   - Creative ID: `{creative_id}`
   - Ad Campaign ID: `{ad_campaign_id}`

## 📊 Пример работы системы

### Шаг 1: Пользователь кликает на рекламу
```
https://www.dailyrewards.site/?zoneid=12345&cost=0.05&clickid=abc123&campaignid=789&bannerid=456
```

### Шаг 2: Лендинг обрабатывает параметры
- Читает `zoneid=12345`
- Создает `source=12345`
- Удаляет `zoneid` из списка параметров
- Добавляет `currency=usd`

### Шаг 3: Редирект на Keitaro
```
https://veotrustkol.com/NMVTN7sQ?source=12345&cost=0.05&clickid=abc123&campaignid=789&bannerid=456&currency=usd
```

### Шаг 4: Keitaro получает данные
- ✅ `source` = 12345 (из zoneid)
- ✅ `cost` = 0.05
- ✅ `currency` = usd
- ✅ `external_id` = abc123 (из clickid)
- ✅ `creative_id` = 456 (из bannerid)
- ✅ `ad_campaign_id` = 789 (из campaignid)

## 🧪 Как проверить, что всё работает

### 1. Тестовый URL:
```
https://www.dailyrewards.site/?zoneid=TEST123&cost=0.05&clickid=test_click&campaignid=999&bannerid=888
```

### 2. Откройте консоль браузера (F12)

Вы увидите детальный лог:
```
═══════════════════════════════════════════════════
🔍 UTM TRACKING DEBUG
═══════════════════════════════════════════════════
📍 Current Page URL: https://www.dailyrewards.site/?zoneid=TEST123&cost=0.05...
📊 URL Search Params: ?zoneid=TEST123&cost=0.05&clickid=test_click...

📦 Parsed Parameters: {zoneid: "TEST123", cost: "0.05", clickid: "test_click", ...}

🎯 Keitaro Parameter Mapping:
  ✓ keyword      = (not set)
  ✓ cost         = 0.05
  ✓ currency     = usd (hardcoded)
  ✓ external_id  = test_click ← from "clickid"
  ✓ creative_id  = 888 ← from "bannerid"
  ✓ ad_campaign_id = 999 ← from "campaignid"
  ✓ source       = TEST123 ← from "zoneid"
  ✓ sub_id_1     = (not set)

🔗 Final Offer URL: https://veotrustkol.com/NMVTN7sQ?cost=0.05&clickid=test_click&campaignid=999&bannerid=888&source=TEST123&currency=usd
```

### 3. Проверьте в Keitaro

После клика на коробку и редиректа, в статистике Keitaro должно появиться:
- **Источник (Source):** TEST123 ← должен быть ваш zoneid, НЕ домен!
- **Стоимость:** 0.05
- **External ID:** test_click

## ❌ Частые ошибки

### Ошибка 1: В Keitaro source = домен вместо zoneid
**Причина:** Старая версия скрипта не делала маппинг `zoneid → source`
**Решение:** Убедитесь, что используете актуальную версию script.js с маппингом

### Ошибка 2: Передаются оба параметра (zoneid И source)
**Причина:** Старая версия скрипта не удаляла zoneid после маппинга
**Решение:** Обновите script.js до версии с `delete params.zoneid`

### Ошибка 3: currency не передается
**Причина:** Рекламная сеть не передает currency, но Keitaro требует
**Решение:** ✅ Уже исправлено! Скрипт автоматически добавляет `currency=usd`

## 🔄 Автореддирект

Лендинг имеет встроенный автореддирект:
- Пользователь выбирает коробку → появляется модальное окно
- Если кликает кнопку "ENTER NOW" → редирект сразу
- Если НЕ кликает → автоматический редирект через **3 секунды**

Это гарантирует, что все пользователи попадут на оффер, даже если не нажмут кнопку.

## 📝 Техническая информация

### Файлы системы:
- **script.js** — обработка UTM параметров, автореддирект
- **index.html** — структура лендинга
- **styles.css** — адаптивный дизайн

### Базовый URL оффера (Keitaro):
```javascript
var baseOfferUrl = 'https://veotrustkol.com/NMVTN7sQ';
```

Если нужно изменить URL оффера, отредактируйте эту строку в `script.js` (строка 35).

## 🆘 Поддержка

Если возникают проблемы:
1. Проверьте консоль браузера на наличие ошибок
2. Убедитесь, что все параметры передаются в URL
3. Проверьте логи в Keitaro
4. Убедитесь, что макросы рекламной сети настроены правильно

---

**Последнее обновление:** 2025-11-28
**Версия:** 1.0
